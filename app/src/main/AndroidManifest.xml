<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.equipmentcheckoutsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReportActivity"></activity>
        <activity android:name=".MaterialsRequestActivity" />
        <activity android:name=".EquipmentRequestActivity" />
        <activity android:name=".ManagementActivity" />
        <activity android:name=".MaterialsDepartmentActivity" />
        <activity android:name=".MaterialsWareHouseActivity" />
        <activity android:name=".CheckInActivity" />
        <activity android:name=".EquipmentDepartmentActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>